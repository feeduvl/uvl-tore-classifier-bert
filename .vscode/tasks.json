{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mlflow",
            "type": "shell",
            "command": "mlflow server"
        },
        {
            "label": "temp_cleanup",
            "type": "shell",
            "command": "rm -rf ./src/data/temp"
        }
    ]
}
